// Quiz scraping placeholder — kept for future use if reliable sources become available.
// Currently all major Hindi quiz sites (AffairsCloud, GKToday) stopped publishing daily
// current affairs quizzes in 2025. Gemini AI (1 call/day, cached) is used instead.
import { MCQ } from './mcq';

export async function scrapeQuizzes(): Promise<MCQ[]> {
  return [];
}
